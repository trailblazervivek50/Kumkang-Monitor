export interface ProjectManagerInfo {
  name: string;
  designation: string;
  email: string;
  phone: string;
  department: string;
  project: string;
  status: 'Active' | 'Inactive';
  photoUrl: string;
}
